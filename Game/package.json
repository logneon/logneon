{
  "name": "alpine-2091",
  "version": "1.0.0",
  "private": true,
  "description": "A playable alpine sci-fi racing experience",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --input-type=module --check < game.js && node --check environment.mjs && node --check server.js",
    "test": "node --test physics.test.mjs"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}
